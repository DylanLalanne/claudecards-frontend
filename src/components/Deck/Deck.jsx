import {useState} from 'react'

function Deck(){
    //mode = variable whatever i last put in
    //setMode = assiging new value to mode, and tell react to rerender such that it applies changes
    const [mode, setMode] = useState
}